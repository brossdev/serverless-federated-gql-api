{
  "name": "sls-gql-fed",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "./gateway",
    "./management",
    "frontend"
  ],
  "scripts": {
    "federation": "concurrently -c \"#4EABD4,#3B22B3\" -k npm:federation:*",
    "federation:management": "npm run start -w management",
    "federation:gateway": "rover supergraph compose --config .supergraph.yaml > dev-schema.graphql && npm run start -w gateway",
    "federation:frontend": "npm run start -w frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.3.0"
  },
  "devDependencies": {
    "@apollo/rover": "^0.4.0"
  }
}
